# Required only for the live agent path (agent/bulletin_agent.py's
# generate_bulletin_live). Without this, the API falls back to
# retrieval-only bulletins with no LLM call.
ANTHROPIC_API_KEY=your_key_here
